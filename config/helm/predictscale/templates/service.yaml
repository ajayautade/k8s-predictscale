apiVersion: v1
kind: Service
metadata:
  name: {{ include "predictscale.fullname" . }}
  labels:
    {{- include "predictscale.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "predictscale.selectorLabels" . | nindent 4 }}
