apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: predictscale

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - rbac.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app.kubernetes.io/part-of: k8s-predictscale
  app.kubernetes.io/managed-by: kustomize
