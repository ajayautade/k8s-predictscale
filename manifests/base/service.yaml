apiVersion: v1
kind: Service
metadata:
  name: predictscale
  namespace: predictscale
  labels:
    app: predictscale
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: predictscale
