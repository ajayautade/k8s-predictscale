apiVersion: apps/v1
kind: Deployment
metadata:
  name: predictscale
spec:
  template:
    spec:
      containers:
        - name: predictscale
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "2Gi"
